public class GameSaveLoad {

    public static void save(GameLevel level, String fileName) {
        // code to save the game level to the specified file
    }

    public static GameLevel load(String fileName) {
        // code to load the game level from the specified file
        // and return it as a GameLevel object
        return null;
    }
}
